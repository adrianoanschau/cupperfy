/** Domínio canônico de produção. */
export const APP_DOMAIN = 'cupperfy.com';

/** Origem absoluta do site em produção. */
export const APP_PRODUCTION_ORIGIN = `https://${APP_DOMAIN}`;

/** E-mail oficial (contato, parcerias e Reply-To). */
export const OFFICIAL_EMAIL = `contato@${APP_DOMAIN}`;

export const APP_NAME = 'Cupperfy';

export const APP_DESCRIPTION =
  'Resultado com prova e uma comunidade que continua. Lista de interesse da Cupperfy — o primeiro campeonato é de EA FC, em um único dia.';

/** Splash e status bar do PWA — `ink-950`. */
export const APP_THEME_COLOR = '#1a1512';
