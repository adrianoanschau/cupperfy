# packages/

Pacotes compartilhados do monorepo (`@leaguefy/*`).

| Pacote | Status |
|---|---|
| `config-typescript` | Placeholder |
| (futuro) `ui`, tipos do domínio, etc. | Quando o app existir |

Apps em `apps/` consomem estes pacotes via workspaces Yarn.
